import { PrintDateStringPipe } from './print-date-string.pipe';

describe('PrintDateStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintDateStringPipe();
    expect(pipe).toBeTruthy();
  });
});
